# Pinned to what was verified working this session (2026-07-14).
# Bump deliberately, not accidentally — re-run mcp-servers/tests/test_transport.py
# after any version change before redeploying.
fastmcp==3.4.4
mcp==1.28.1
starlette==1.3.1
uvicorn==0.52.3
httpx==0.28.1
