# <Organisation / Agent Name> — Persistent Memory (template v0.1)

This is a TEMPLATE. It is not itself a valid, ratified identity file —
`<angle-bracket>` placeholders are meant to be replaced before this is
used for real. Once filled in, run it through `validate_memory_files.py`
the same, unmodified way the SynapSys instance (`../../synapsys.md`) is
validated — see `example/` in this folder for a filled-in instance that
already passes.

## Purpose (ratified by <naming your actual ratifying authority>, <date>)
<One sentence: what this organisation/agent exists to do, repeatably.
Keep it to one sentence on purpose — if it needs a paragraph, it isn't
settled yet.>

## Operating discipline
<Name your own maturity ladder — how developed an idea is (SynapSys
uses FORM -> FLOW -> EVOLVE; yours can differ).>
<Name your own evidence ladder — how much proof backs a claim; keep
this a SEPARATE axis from maturity, not the same scale relabelled.>
<State your own asset-declaration discipline if you have one, or say
plainly that you don't yet.>

## Standing rule
Never claim <your ecosystem>'s state from memory alone. Re-fetch
<name your actual canonical source> fresh, every time, before making
the claim.

## What this file is not
Not authority, not canon, not a substitute for <your durable system of
record> or <your equivalent of an always-loaded operational-detail
file>. If content needs to change often, it doesn't belong here — keep
it in deep storage and reference it, don't inline it.
