# <Principal Name / Role> — (template v0.1)

This is a TEMPLATE. `<angle-bracket>` placeholders are meant to be
replaced before this is used for real. See `example/` in this folder
for a filled-in instance that already passes `validate_memory_files.py`
unmodified.

## Role
<Their designation and final-authority scope. State plainly what
channel exists (or doesn't) between this file's holder and any AI
agents/lanes in your environment — e.g. direct API access, relay-only
through a human, no channel at all. Don't assume a richer connection
than what's actually true.>

## Observed this session (context, not authority)
<Empty at instantiation. Filled in only with real, dated observations
as they accumulate — preferences, corrections, constraints actually
seen in a real session, not assumed in advance. Never treated as a
fixed profile; corrected in place when wrong.>

## What this file is not
Not a grant of authority beyond what's stated above. An observation
record, corrected in place when wrong — not a fixed profile.
