# Example Principal — (worked example v0.1)

This is a WORKED EXAMPLE, not a real person. It exists to prove the
two-file pattern is genuinely portable — validated by the exact same,
unmodified `validate_memory_files.py` used for `steward.md`.

## Role
Final authority for Example Collaborator Co's delivery decisions.
Relay-only channel to any AI agents in this environment as of this
writing — no direct session-to-session link exists yet; stated plainly
rather than assumed richer than it is.

## Observed this session (context, not authority)
No real session has been observed yet — this section starts empty at
instantiation and is filled in only with real, dated observations as
they accumulate, the same discipline as the SynapSys instance.

## What this file is not
Not a grant of authority beyond what's stated above. An observation
record, corrected in place when wrong — not a fixed profile.
